# Django Settings
SECRET_KEY=your-secret-key-here
DEBUG=True

# Email Configuration (Gmail SMTP)
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=noreply@ofori-blog.com

# Database (SQLite is default, no configuration needed)
# For production, you might want to use PostgreSQL or MySQL

# Domain Configuration
# ALLOWED_HOSTS is already configured in settings.py
# For production: blog.digitalrepublic.space
# For development: 127.0.0.1, localhost
